get:
  tags:
    - admin
  summary: Get database information
  responses:
    "200":
      description: |
        A JSON object with database status information.
        Each database configured will have an entry here with information about its runtime status
      content:
        application/json:
          schema:
            properties: {}
            additionalProperties: true