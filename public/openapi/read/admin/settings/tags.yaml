get:
  tags:
    - admin
  summary: Get tag settings
  responses:
    "200":
      description: ""
      content:
        application/json:
          schema:
            allOf:
              - type: object
                properties:
                  title:
                    type: string
              - $ref: ../../../components/schemas/CommonProps.yaml#/CommonProps